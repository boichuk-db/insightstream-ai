{
  "name": "insightstream-ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "kill": "kill-port 3000 3001",
    "predev": "pnpm kill",
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "kill-port": "^2.0.1",
    "prettier": "^3.2.5",
    "turbo": "^2.1.3",
    "typescript": "^6.0.2"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=18"
  }
}
