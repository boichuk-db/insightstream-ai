# Template for local dev test login. Copy to `.env.seed` (gitignored) and run:
#   pnpm --filter api seed:test-user
# The real .env.seed is never committed; only this template is tracked.
TEST_USER_EMAIL=dev@insightstream.local
TEST_USER_PASSWORD=change-me
